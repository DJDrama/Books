2.5.2 "try" as an expression

	- Using try as an expression
		-> help you make code a bit more concise by avoiding the introduction of additional intermediate variables
		-> allows you to easily assign fallback values or return from the enclosing function outright.